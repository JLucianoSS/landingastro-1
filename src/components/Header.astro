---
// Header component
---

<header
  class="fixed top-0 left-0 w-full z-50 bg-transparent backdrop-blur-sm"
  style="opacity: 0; transform: translateY(-100px);"
>
  <div
    class="container mx-auto px-8 pt-6 pb-3 flex justify-between items-center"
  >
    <!-- Logo -->
    <div
      class="logo-container relative"
      style="opacity: 0; transform: translateX(-50px);"
    >
      <!-- Logo inicial - Visible en el hero -->
      <a href="/">
        <img
          src="/images/logo-hero.png"
          alt="LandingAstro"
          class="logo-hero w-60 h-auto transition-all duration-500"
        />
        <!-- Logo después del scroll - Se muestra después del hero -->
        <img
          src="/images/logo-scroll.png"
          alt="LandingAstro"
          class="logo-scroll w-60 h-auto transition-all duration-500 opacity-0 absolute top-0 left-0"
        />
      </a>
    </div>

    <!-- Menú desplegable para desktop -->
    <div class="hidden md:block flex items-center space-x-4">
      <!-- Botón Únete -->
      <a
        href="#"
        class="btn-unete inline-flex items-center px-6 py-3 bg-white text-dark rounded-full font-semibold text-base transition-all duration-300 transform hover:scale-105 shadow-lg"
        style="opacity: 0; transform: translateY(-20px);"
      >
        <svg
              class="w-5 h-5 mr-2"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
        </svg>
        <span class="font-stolzl">Centro de atención</span>
      </a>
      
      <button
        class="menu-trigger relative group"
        style="opacity: 0; transform: translateY(-20px);"
      >
        <div
          class="font-stolzl menu-circle w-28 h-28 bg-secondary rounded-full opacity-0 flex items-center justify-center transition-all duration-300 group-hover:scale-105 group-hover:brightness-110"
          style="background-color: #062BED;"
        >
          <span class="menu-text text-white font-medium text-base tracking-wide"
            >Menú</span
          >
        </div>
      </button>

      <!-- Menú desplegable -->
      <div
        class="menu-dropdown absolute top-full left-0 right-0 bg-white shadow-lg opacity-0 pointer-events-none transform -translate-y-4 transition-all duration-300"
      >
        <div class="container mx-auto px-4 py-8">
          <nav class="grid grid-cols-2 lg:grid-cols-3 gap-8">
            <div class="space-y-4">
              <h3 class="text-lg font-bold text-dark mb-4">Navegación</h3>
              <a
                href="#identidad"
                class="block text-dark hover:text-secondary transition-colors"
                >Identidad</a
              >
              <a
                href="#servicios"
                class="block text-dark hover:text-secondary transition-colors"
                >Servicios</a
              >
              <a
                href="#soluciones"
                class="block text-dark hover:text-secondary transition-colors"
                >Soluciones</a
              >
            </div>
            <div class="space-y-4">
              <h3 class="text-lg font-bold text-dark mb-4">Empresa</h3>
              <a
                href="#alianzas"
                class="block text-dark hover:text-secondary transition-colors"
                >Alianzas</a
              >
              <a
                href="#equipo"
                class="block text-dark hover:text-secondary transition-colors"
                >Equipo</a
              >
              <a
                href="#contacto"
                class="block text-dark hover:text-secondary transition-colors"
                >Contacto</a
              >
            </div>
            <div class="space-y-4">
              <h3 class="text-lg font-bold text-dark mb-4">Contacto</h3>
              <p class="text-dark/80">info@landingastro.com</p>
              <p class="text-dark/80">+34 123 456 789</p>
            </div>
          </nav>
        </div>
      </div>
    </div>

    <!-- Botón de menú móvil -->
    <div class="md:hidden flex items-center space-x-4">
      
      <button
        class="menu-trigger-mobile relative group"
        aria-label="Abrir menú"
      >
        <div
          class="font-stolzl menu-circle-mobile w-16 h-16 bg-secondary rounded-full opacity-0 flex items-center justify-center transition-all duration-300 group-hover:scale-105 group-hover:brightness-110"
          style="background-color: #062BED;"
        >
          <span
            class="menu-text-mobile text-white font-medium text-sm tracking-wide"
            >Menú</span
          >
        </div>
      </button>
    </div>
  </div>
</header>

<!-- Menú móvil de pantalla completa estilo CreativeSpirit -->
<div
  class="mobile-menu-overlay fixed inset-0 bg-white z-40 transform translate-y-full transition-transform duration-500 ease-in-out md:hidden"
>
  <!-- Elementos decorativos circulares -->
  <div
    class="absolute top-[180px] left-0 w-96 h-[150px] bg-[#062bed] rounded-full -translate-x-1/2 -translate-y-1/2 pointer-events-none"
  >
  </div>
  <div
    class="absolute bottom-0 right-0 w-[300px] h-[300px] bg-[#062bed] rounded-full translate-x-1/2 translate-y-1/2 pointer-events-none"
  >
  </div>

  <div class="relative h-full flex flex-col">
    <!-- Header del menú -->
    <div class="flex justify-between items-center p-6">
      <img
        src="/images/logo-scroll.png"
        alt="LandingAstro"
        class="w-48 h-auto"
      />
      <button
        class="mobile-menu-close w-16 h-16 bg-secondary rounded-full flex items-center justify-center transform rotate-12"
      >
        <span class="text-white font-bold text-base">Cerrar</span>
      </button>
    </div>

    <!-- Navegación principal -->
    <div class="flex-1 flex items-center justify-end pr-8">
      <nav class="space-y-8">
        <a
          href="#identidad"
          class="mobile-menu-link flex items-center space-x-4 group"
        >
          <div
            class="w-8 h-8 border border-secondary rounded-full flex items-center justify-center"
          >
            <span class="text-secondary text-sm font-medium">01</span>
          </div>
          <span
            class="text-black text-4xl font-light group-hover:text-secondary transition-colors duration-300"
            >Identidad</span
          >
        </a>

        <a
          href="#servicios"
          class="mobile-menu-link flex items-center space-x-4 group"
        >
          <div
            class="w-8 h-8 border border-secondary rounded-full flex items-center justify-center"
          >
            <span class="text-secondary text-sm font-medium">02</span>
          </div>
          <span
            class="text-black text-4xl font-light group-hover:text-secondary transition-colors duration-300"
            >Servicios</span
          >
        </a>

        <a
          href="#soluciones"
          class="mobile-menu-link flex items-center space-x-4 group"
        >
          <div
            class="w-8 h-8 border border-secondary rounded-full flex items-center justify-center"
          >
            <span class="text-secondary text-sm font-medium">03</span>
          </div>
          <span
            class="text-black text-4xl font-light group-hover:text-secondary transition-colors duration-300"
            >Soluciones</span
          >
        </a>

        <a
          href="#alianzas"
          class="mobile-menu-link flex items-center space-x-4 group"
        >
          <div
            class="w-8 h-8 border border-secondary rounded-full flex items-center justify-center"
          >
            <span class="text-secondary text-sm font-medium">04</span>
          </div>
          <span
            class="text-black text-4xl font-light group-hover:text-secondary transition-colors duration-300"
            >Alianzas</span
          >
        </a>

        <a
          href="#contacto"
          class="mobile-menu-link flex items-center space-x-4 group"
        >
          <div
            class="w-8 h-8 border border-secondary rounded-full flex items-center justify-center"
          >
            <span class="text-secondary text-sm font-medium">05</span>
          </div>
          <span
            class="text-black text-4xl font-light group-hover:text-secondary transition-colors duration-300"
            >Contacto</span
          >
        </a>
      </nav>
    </div>

    <!-- Contenedor con boton centro de atención -->
    <div class="px-6 flex">
      <a
        href="#"
        class="inline-flex items-center px-5 py-[8px] bg-[#050835] text-white rounded-full font-semibold text-base transition-all duration-300 transform hover:scale-105 shadow-lg"
      >
          <svg
                class="w-5 h-5 mr-2"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
          </svg>
          <span class="font-stolzl">Centro de atención</span>
        </a>
    </div>

    <!-- Footer con redes sociales -->
    <div class="p-6">
      <div class="flex space-x-6">
        <!-- FACEBOOK -->
        <a
          href="#"
          class="text-black hover:text-secondary transition-colors duration-300"
        >
          <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
            <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
          </svg>
        </a>
        <!-- INSTAGRAM -->
        <a
          href="#"
          class="text-black hover:text-secondary transition-colors duration-300"
        >
          <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
            <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/>
          </svg>
        </a>
        <!-- LINKEDIN -->
        <a
          href="#"
          class="text-black hover:text-secondary transition-colors duration-300"
        >
          <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
            <path
              d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"
            ></path>
          </svg>
        </a>
      </div>
    </div>
  </div>
</div>

<!-- Menú de pantalla completa para desktop estilo CreativeSpirit -->
<div
  class="desktop-menu-overlay fixed inset-0 bg-white z-40 transform translate-y-full transition-transform duration-500 ease-in-out hidden md:block"
>
  <!-- Elementos decorativos circulares -->
  <div
    class="absolute top-0 left-0 w-[500px] h-[400px] bg-secondary rounded-full -translate-x-1/2 translate-y-32 pointer-events-none"
  >
  </div>
  <div
    class="absolute bottom-0 right-0 w-[700px] h-[700px] bg-secondary rounded-full translate-x-1/2 translate-y-1/2 pointer-events-none"
  >
  </div>

  <div class="relative h-full flex flex-col">
    <!-- Header del menú -->
    <div class="flex justify-between items-center p-6">
      <img
        src="/images/logo-scroll.png"
        alt="LandingAstro"
        class="w-48 h-auto"
      />
      <button
        class="desktop-menu-close w-20 h-20 bg-secondary rounded-full flex items-center justify-center transform rotate-12 transition-all duration-300 hover:scale-110 hover:rotate-0 hover:bg-dark hover:shadow-lg group"
      >
        <span
          class="text-white font-bold text-lg transition-colors duration-300 group-hover:text-white"
          >Cerrar</span
        >
      </button>
    </div>

    <!-- Navegación principal -->
    <div class="flex-1 flex items-center justify-center">
      <nav class="space-y-8">
        <a
          href="#identidad"
          class="desktop-menu-link flex items-center space-x-5 group"
        >
          <div
            class="w-9 h-9 border border-secondary rounded-full flex items-center justify-center"
          >
            <span class="text-secondary text-sm font-medium">01</span>
          </div>
          <span
            class="text-black text-5xl font-light group-hover:text-secondary transition-colors duration-300"
            >Identidad</span
          >
        </a>

        <a
          href="#servicios"
          class="desktop-menu-link flex items-center space-x-5 group"
        >
          <div
            class="w-9 h-9 border border-secondary rounded-full flex items-center justify-center"
          >
            <span class="text-secondary text-sm font-medium">02</span>
          </div>
          <span
            class="text-black text-5xl font-light group-hover:text-secondary transition-colors duration-300"
            >Servicios</span
          >
        </a>

        <a
          href="#soluciones"
          class="desktop-menu-link flex items-center space-x-5 group"
        >
          <div
            class="w-9 h-9 border border-secondary rounded-full flex items-center justify-center"
          >
            <span class="text-secondary text-sm font-medium">03</span>
          </div>
          <span
            class="text-black text-5xl font-light group-hover:text-secondary transition-colors duration-300"
            >Soluciones</span
          >
        </a>

        <a
          href="#alianzas"
          class="desktop-menu-link flex items-center space-x-5 group"
        >
          <div
            class="w-9 h-9 border border-secondary rounded-full flex items-center justify-center"
          >
            <span class="text-secondary text-sm font-medium">04</span>
          </div>
          <span
            class="text-black text-5xl font-light group-hover:text-secondary transition-colors duration-300"
            >Alianzas</span
          >
        </a>

        <a
          href="#equipo"
          class="desktop-menu-link flex items-center space-x-5 group"
        >
          <div
            class="w-9 h-9 border border-secondary rounded-full flex items-center justify-center"
          >
            <span class="text-secondary text-sm font-medium">05</span>
          </div>
          <span
            class="text-black text-5xl font-light group-hover:text-secondary transition-colors duration-300"
            >Equipo</span
          >
        </a>

        <a
          href="#contacto"
          class="desktop-menu-link flex items-center space-x-5 group"
        >
          <div
            class="w-9 h-9 border border-secondary rounded-full flex items-center justify-center"
          >
            <span class="text-secondary text-sm font-medium">06</span>
          </div>
          <span
            class="text-black text-5xl font-light group-hover:text-secondary transition-colors duration-300"
            >Contacto</span
          >
        </a>
      </nav>
    </div>

    <!-- Footer con redes sociales -->
    <div class="p-6">
      <div class="flex space-x-6">
        <!-- FACEBOOK -->
        <a
          href="#"
          class="text-black hover:text-secondary transition-colors duration-300"
        >
          <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
            <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
          </svg>
        </a>
        <!-- INSTAGRAM -->
        <a
          href="#"
          class="text-black hover:text-secondary transition-colors duration-300"
        >
          <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
            <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/>
          </svg>
        </a>
        <!-- LINKEDIN -->
        <a
          href="#"
          class="text-black hover:text-secondary transition-colors duration-300"
        >
          <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
            <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
          </svg>
        </a>
      </div>
    </div>
  </div>
</div>

<!-- Loader completo que cubre toda la página -->
<div
  class="page-loader fixed inset-0 bg-white z-[100] flex items-center justify-center"
>
  <div
    class="loading-circle bg-secondary rounded-full flex items-center justify-center"
    style="width: 90vw; height: 90vw; max-width: 90vh; max-height: 90vh; transform: scale(0); opacity: 0;"
  >
  </div>
</div>

<script>
  // Función para animar el círculo de carga con CSS puro (fallback)
  function animateLoadingCircle() {
    const loadingCircle = document.querySelector(".loading-circle");
    const menuCircle = document.querySelector(".menu-circle");
    if (!loadingCircle) return;

    // Paso 0: Entrada elegante del círculo
    loadingCircle.style.transition =
      "all 0.6s cubic-bezier(0.68, -0.55, 0.265, 1.55)";
    loadingCircle.style.transform = "translate(-50%, -50%) scale(1)";
    loadingCircle.style.opacity = "1";

    // Paso 1: Pausa breve y luego hacer el círculo más pequeño
    setTimeout(() => {
      loadingCircle.style.transition =
        "transform 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94)";
      loadingCircle.style.transform = "translate(-50%, -50%) scale(0.1)";
    }, 900);

    // Paso 2: Mover hacia el menú sin pausa
    setTimeout(() => {
      loadingCircle.style.transition =
        "all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94)";
      loadingCircle.style.left = "auto";
      loadingCircle.style.top = "24px";
      loadingCircle.style.right = "120px";
      loadingCircle.style.transform = "translate(0, 0) scale(0.04)";

      // Paso 3: Mostrar el menú y ocultar el círculo de carga
      setTimeout(() => {
        if (menuCircle) {
          menuCircle.style.opacity = "1";
        }
        loadingCircle.style.opacity = "0";
        setTimeout(() => {
          loadingCircle.style.display = "none";
        }, 100);
      }, 200);
    }, 1200);
  }

  // Función para animar el header
  function animateHeader() {
    const header = document.querySelector("header");
    const logo = document.querySelector(".logo-container");
    const navLinks = document.querySelectorAll("nav a");

    if (header) {
      header.style.transition = "all 0.8s ease-out";
      header.style.transform = "translateY(0)";
      header.style.opacity = "1";
    }

    if (logo) {
      setTimeout(() => {
        logo.style.transition = "all 0.6s ease-out";
        logo.style.transform = "translateX(0)";
        logo.style.opacity = "1";
      }, 500);
    }

    navLinks.forEach((link, index) => {
      setTimeout(
        () => {
          link.style.transition = "all 0.4s ease-out";
          link.style.transform = "translateY(0)";
          link.style.opacity = "1";
        },
        800 + index * 100
      );
    });
  }

  // Función principal de inicialización (solo para header y elementos después del loading)
  function initAnimations() {
    // Intentar usar GSAP si está disponible
    if (typeof gsap !== "undefined" && window.gsapReady) {
      console.log("🎬 Usando GSAP para animaciones del header");

      // Solo animaciones del header - MÁXIMA VELOCIDAD
      gsap.to("header", {
        y: 0,
        opacity: 1,
        duration: 0.15,
        delay: 0.8,
        ease: "power2.out",
      });

      gsap.to(".logo-container", {
        x: 0,
        opacity: 1,
        duration: 0.15,
        delay: 0.85,
        ease: "power2.out",
      });

      gsap.to(".btn-unete", {
        y: 0,
        opacity: 1,
        duration: 0.15,
        delay: 0.9,
        ease: "power2.out",
      });

      gsap.to(".menu-trigger", {
        y: 0,
        opacity: 1,
        duration: 0.15,
        delay: 0.95,
        ease: "power2.out",
      });

      // Asegurar que el círculo del menú también sea visible
      gsap.to(".menu-circle", {
        opacity: 1,
        scale: 1,
        duration: 0.15,
        delay: 0.95,
        ease: "power2.out",
        onComplete: () => {
          // ESPERAR que termine toda la animación del header y LUEGO hacer la animación elegante
          setTimeout(() => {
            // Animación elegante final - pulso como latido del corazón
            gsap.to(".menu-circle", {
              scale: 1.15,
              duration: 1.2,
              ease: "power2.inOut",
              yoyo: true,
              repeat: 2,
              repeatDelay: 0.3,
            });

            // Animación del texto dentro del círculo
            gsap.from(".menu-text", {
              scale: 0.8,
              opacity: 0,
              duration: 0.5,
              ease: "back.out(1.7)",
            });
          }, 500); // Esperar 500ms después de que aparezca el menú
        },
      });

      // También animar el círculo del menú móvil
      gsap.to(".menu-circle-mobile", {
        opacity: 1,
        scale: 1,
        duration: 0.15,
        delay: 0.95,
        ease: "power2.out",
      });

    } else {
      console.log("🎨 Usando CSS puro para animaciones del header");

      // Configurar estados iniciales del header
      const header = document.querySelector("header");
      const logo = document.querySelector(".logo-container");
      const menuTrigger = document.querySelector(".menu-trigger");
      const btnUnete = document.querySelector(".btn-unete");

      if (header) {
        header.style.transform = "translateY(-100px)";
        header.style.opacity = "0";
      }

      if (logo) {
        logo.style.transform = "translateX(-50px)";
        logo.style.opacity = "0";
      }

      if (menuTrigger) {
        menuTrigger.style.transform = "translateY(-20px)";
        menuTrigger.style.opacity = "0";
      }

      if (btnUnete) {
        btnUnete.style.transform = "translateY(-20px)";
        btnUnete.style.opacity = "0";
      }

      const menuCircleElement = document.querySelector(".menu-circle");
      if (menuCircleElement) {
        menuCircleElement.style.opacity = "0";
        menuCircleElement.style.transform = "scale(0)";
      }

      // Solo animar el header - MÁXIMA VELOCIDAD
      setTimeout(animateHeader, 800);

      // Animar el botón Únete por separado - MÁXIMA VELOCIDAD
      setTimeout(() => {
        if (btnUnete) {
          btnUnete.style.transition = "all 0.15s ease-out";
          btnUnete.style.opacity = "1";
          btnUnete.style.transform = "translateY(0)";
        }
      }, 900);

      // Animar el menú por separado - MÁXIMA VELOCIDAD
      setTimeout(() => {
        if (menuTrigger) {
          menuTrigger.style.transition = "all 0.15s ease-out";
          menuTrigger.style.opacity = "1";
          menuTrigger.style.transform = "translateY(0)";
        }

        if (menuCircleElement) {
          menuCircleElement.style.transition = "all 0.15s ease-out";
          menuCircleElement.style.opacity = "1";
          menuCircleElement.style.transform = "scale(1)";

          // ESPERAR que termine toda la animación del header y LUEGO hacer la animación elegante
          setTimeout(() => {
            // Pulso como latido del corazón - más lento y rítmico
            menuCircleElement.style.transition =
              "transform 1.2s cubic-bezier(0.68, -0.55, 0.265, 1.55)";

            // Primer latido
            menuCircleElement.style.transform = "scale(1.15)";
            setTimeout(() => {
              menuCircleElement.style.transform = "scale(1)";
              // Segundo latido después de una pausa
              setTimeout(() => {
                menuCircleElement.style.transform = "scale(1.15)";
                setTimeout(() => {
                  menuCircleElement.style.transform = "scale(1)";
                }, 1200);
              }, 300);
            }, 1200);

            // Animar el texto dentro del círculo
            const menuText = menuCircleElement.querySelector(".menu-text");
            if (menuText) {
              menuText.style.transition =
                "all 0.5s cubic-bezier(0.68, -0.55, 0.265, 1.55)";
              menuText.style.transform = "scale(0.8)";
              menuText.style.opacity = "0";

              setTimeout(() => {
                menuText.style.transform = "scale(1)";
                menuText.style.opacity = "1";
              }, 50);
            }
          }, 500); // Esperar 500ms después de que aparezca el menú
        }
      }, 950);
    }
  }

  // Toggle del menú de pantalla completa de desktop
  const menuTrigger = document.querySelector(".menu-trigger");
  const desktopMenuOverlay = document.querySelector(".desktop-menu-overlay");
  const desktopMenuClose = document.querySelector(".desktop-menu-close");
  const menuText = document.querySelector(".menu-text");
  const desktopMenuLinks = document.querySelectorAll(".desktop-menu-link");

  let isDesktopMenuOpen = false;

  // Función para scroll suave con easing personalizado
  function smoothScrollTo(targetPosition, duration) {
    const startPosition = window.pageYOffset;
    const distance = targetPosition - startPosition;
    let startTime = null;

    // Función de easing suave (ease-in-out)
    function easeInOutCubic(t) {
      return t < 0.5 ? 4 * t * t * t : (t - 1) * (2 * t - 2) * (2 * t - 2) + 1;
    }

    function animation(currentTime) {
      if (startTime === null) startTime = currentTime;
      const timeElapsed = currentTime - startTime;
      const progress = Math.min(timeElapsed / duration, 1);

      const ease = easeInOutCubic(progress);
      window.scrollTo(0, startPosition + distance * ease);

      if (timeElapsed < duration) {
        requestAnimationFrame(animation);
      }
    }

    requestAnimationFrame(animation);
  }

  function openDesktopMenu() {
    isDesktopMenuOpen = true;
    desktopMenuOverlay?.classList.remove("translate-y-full");
    document.body.style.overflow = "hidden";

    // Cambiar texto del botón
    if (menuText) {
      menuText.textContent = "Cerrar";
    }
  }

  function closeDesktopMenu() {
    isDesktopMenuOpen = false;
    desktopMenuOverlay?.classList.add("translate-y-full");
    document.body.style.overflow = "auto";

    // Cambiar texto del botón
    if (menuText) {
      menuText.textContent = "Menú";
    }
  }

  // Abrir menú de desktop
  menuTrigger?.addEventListener("click", () => {
    if (!isDesktopMenuOpen) {
      openDesktopMenu();
    } else {
      closeDesktopMenu();
    }
  });

  // Cerrar menú con botón X
  desktopMenuClose?.addEventListener("click", closeDesktopMenu);

  // Cerrar menú al hacer clic en un enlace
  desktopMenuLinks.forEach((link) => {
    link.addEventListener("click", (e) => {
      e.preventDefault();
      const targetId = link.getAttribute("href");

      // Cerrar menú primero
      closeDesktopMenu();

      // Scroll suave a la sección después de cerrar el menú
      setTimeout(() => {
        if (targetId && targetId.startsWith("#")) {
          const targetElement = document.querySelector(targetId);
          if (targetElement) {
            // Calcular la posición del elemento considerando el header fijo
            const headerHeight = 60; // Altura aproximada del header
            const elementPosition = targetElement.offsetTop - headerHeight;

            // Scroll suave personalizado con easing
            smoothScrollTo(elementPosition, 1200); // 1.2 segundos de duración
          }
        }
      }, 500); // Esperar a que termine la animación de cierre
    });
  });

  // Cerrar menú al hacer clic fuera (en el overlay)
  desktopMenuOverlay?.addEventListener("click", (e) => {
    if (e.target === desktopMenuOverlay) {
      closeDesktopMenu();
    }
  });

  // Toggle del menú móvil de pantalla completa
  const menuToggleMobile = document.querySelector(".menu-trigger-mobile");
  const mobileMenuOverlay = document.querySelector(".mobile-menu-overlay");
  const mobileMenuClose = document.querySelector(".mobile-menu-close");
  const menuTextMobile = document.querySelector(".menu-text-mobile");
  const mobileMenuLinks = document.querySelectorAll(".mobile-menu-link");

  let isMenuOpen = false;

  function openMobileMenu() {
    isMenuOpen = true;
    mobileMenuOverlay?.classList.remove("translate-y-full");
    document.body.style.overflow = "hidden";

    // Cambiar texto del botón
    if (menuTextMobile) {
      menuTextMobile.textContent = "Cerrar";
    }
  }

  function closeMobileMenu() {
    isMenuOpen = false;
    mobileMenuOverlay?.classList.add("translate-y-full");
    document.body.style.overflow = "auto";

    // Cambiar texto del botón
    if (menuTextMobile) {
      menuTextMobile.textContent = "Menú";
    }
  }

  // Abrir menú
  menuToggleMobile?.addEventListener("click", () => {
    if (!isMenuOpen) {
      openMobileMenu();
    } else {
      closeMobileMenu();
    }
  });

  // Cerrar menú con botón X
  mobileMenuClose?.addEventListener("click", closeMobileMenu);

  // Cerrar menú al hacer clic en un enlace
  mobileMenuLinks.forEach((link) => {
    link.addEventListener("click", (e) => {
      e.preventDefault();
      const targetId = link.getAttribute("href");

      // Cerrar menú primero
      closeMobileMenu();

      // Scroll suave a la sección después de cerrar el menú
      setTimeout(() => {
        if (targetId && targetId.startsWith("#")) {
          const targetElement = document.querySelector(targetId);
          if (targetElement) {
            // Calcular la posición del elemento considerando el header fijo
            const headerHeight = 60; // Altura aproximada del header
            const elementPosition = targetElement.offsetTop - headerHeight;

            // Scroll suave personalizado con easing
            smoothScrollTo(elementPosition, 1200); // 1.2 segundos de duración
          }
        }
      }, 500); // Esperar a que termine la animación de cierre
    });
  });

  // Cerrar menú al hacer clic fuera (en el overlay)
  mobileMenuOverlay?.addEventListener("click", (e) => {
    if (e.target === mobileMenuOverlay) {
      closeMobileMenu();
    }
  });

  // Efecto de scroll para el header transparente con cambio de logos
  let lastScrollY = window.scrollY;

  function updateHeaderOnScroll() {
    const currentScrollY = window.scrollY;
    const header = document.querySelector("header");
    const headerContainer = document.querySelector(".container");
    const logoHero = document.querySelector(".logo-hero");
    const logoScroll = document.querySelector(".logo-scroll");
    const menuCircle = document.querySelector(".menu-circle");
    const menuCircleMobile = document.querySelector(".menu-circle-mobile");
    const btnUnete = document.querySelector(".btn-unete");

    if (header && headerContainer) {
      if (currentScrollY > 100) {
        // Cuando se hace scroll, hacer el header más visible y compacto
        header.style.backgroundColor = "rgba(255, 255, 255, 0.95)";
        header.style.backdropFilter = "blur(10px)";
        header.style.borderBottom = "none";

        // Reducir la altura del header
        headerContainer.style.paddingTop = "0.75rem";
        headerContainer.style.paddingBottom = "0.75rem";

        // Cambiar a logo de scroll y hacer más pequeño
        if (logoHero && logoScroll) {
          logoHero.style.opacity = "0";
          logoScroll.style.opacity = "1";
          logoScroll.style.transform = "scale(0.8)";
        }

        // Hacer el botón del menú más pequeño
        if (menuCircle) {
          menuCircle.style.transform = "scale(0.8)";
        }
        if (menuCircleMobile) {
          menuCircleMobile.style.transform = "scale(0.8)";
        }

        // Cambiar el color del botón Únete cuando se hace scroll
        if (btnUnete) {
          btnUnete.style.backgroundColor = "#050835";
          btnUnete.style.color = "white";
          // Agregar clase para hover oscuro
          btnUnete.classList.add("btn-unete-scrolled");
          btnUnete.classList.remove("btn-unete-normal");
        }
      } else {
        // En la parte superior, mantener transparente y altura normal
        header.style.backgroundColor = "transparent";
        header.style.backdropFilter = "blur(4px)";
        header.style.borderBottom = "none";

        // Restaurar altura normal del header
        headerContainer.style.paddingTop = "1.5rem";
        headerContainer.style.paddingBottom = "0.75rem";

        // Mostrar logo del hero y tamaño normal
        if (logoHero && logoScroll) {
          logoHero.style.opacity = "1";
          logoScroll.style.opacity = "0";
          logoScroll.style.transform = "scale(1)";
        }

        // Botón del menú tamaño normal
        if (menuCircle) {
          menuCircle.style.transform = "scale(1)";
        }
        if (menuCircleMobile) {
          menuCircleMobile.style.transform = "scale(1)";
        }

        // Restaurar el color original del botón Únete cuando está en la parte superior
        if (btnUnete) {
          btnUnete.style.backgroundColor = "white";
          btnUnete.style.color = "#1a1a1a";
          // Agregar clase para hover normal
          btnUnete.classList.add("btn-unete-normal");
          btnUnete.classList.remove("btn-unete-scrolled");
        }
      }
    }

    lastScrollY = currentScrollY;
  }

  // Agregar el listener de scroll
  window.addEventListener("scroll", updateHeaderOnScroll);

  // Verificar la posición inicial del scroll al cargar la página
  function checkInitialScrollPosition() {
    updateHeaderOnScroll();
  }

  // Ejecutar al cargar la página
  checkInitialScrollPosition();

  // Iniciar la animación del loader INMEDIATAMENTE
  const pageLoader = document.querySelector(".page-loader");
  const loadingCircle = document.querySelector(".loading-circle");
  const menuCircle = document.querySelector(".menu-circle");

  if (loadingCircle && pageLoader) {
    // Pequeño delay para asegurar que el DOM esté listo, luego iniciar animación
    setTimeout(() => {
      // Paso 1: Mostrar el círculo con entrada elegante y más lenta
      loadingCircle.style.transition =
        "all 0.8s cubic-bezier(0.68, -0.55, 0.265, 1.55)";
      loadingCircle.style.transform = "scale(1)";
      loadingCircle.style.opacity = "1";
    }, 200);

    // Paso 2: Pausa breve para apreciar el círculo (ajustado por el delay inicial)
    setTimeout(() => {
      // Paso 3: Contraer el círculo más suavemente
      loadingCircle.style.transition =
        "transform 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94)";
      loadingCircle.style.transform = "scale(0.1)";

      setTimeout(() => {
        // Paso 4: Mover el círculo al menú con más suavidad
        loadingCircle.style.transition =
          "all 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94)";
        loadingCircle.style.position = "fixed";
        loadingCircle.style.left = "auto";
        loadingCircle.style.right = "120px";
        loadingCircle.style.top = "24px";
        loadingCircle.style.width = "32px";
        loadingCircle.style.height = "32px";
        loadingCircle.style.transform = "scale(1)";

        setTimeout(() => {
          // Paso 5: Desvanecimiento más suave del loader
          pageLoader.style.transition = "opacity 0.5s ease-out";
          pageLoader.style.opacity = "0";

          setTimeout(() => {
            pageLoader.style.display = "none";
            // Transición suave del círculo de carga al menú
            loadingCircle.style.transition = "opacity 0.4s ease-out";
            loadingCircle.style.opacity = "0";

            setTimeout(() => {
              loadingCircle.style.display = "none";
              // Mostrar el círculo del menú permanente con transición suave
              if (menuCircle) {
                menuCircle.style.transition = "all 0.3s ease-out";
                menuCircle.style.opacity = "1";
                menuCircle.style.transform = "scale(1)";
                menuCircle.style.background = "#062BED";
              }

              // También mostrar el círculo del menú móvil
              const menuCircleMobile = document.querySelector(
                ".menu-circle-mobile"
              );
              if (menuCircleMobile) {
                menuCircleMobile.style.transition = "all 0.3s ease-out";
                menuCircleMobile.style.opacity = "1";
                menuCircleMobile.style.transform = "scale(1)";
                menuCircleMobile.style.background = "#062BED";

                // Animación elegante final - pulso que indica que está listo
                setTimeout(() => {
                  menuCircle.style.transition =
                    "transform 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55)";
                  menuCircle.style.transform = "scale(1.05)";

                  setTimeout(() => {
                    menuCircle.style.transform = "scale(1)";
                  }, 400);

                  // Animar el texto dentro del círculo
                  const menuText = menuCircle.querySelector(".menu-text");
                  if (menuText) {
                    menuText.style.transition =
                      "all 0.5s cubic-bezier(0.68, -0.55, 0.265, 1.55)";
                    menuText.style.transform = "scale(0.8)";
                    menuText.style.opacity = "0";

                    setTimeout(() => {
                      menuText.style.transform = "scale(1)";
                      menuText.style.opacity = "1";
                    }, 50);
                  }
                }, 500); // Esperar 500ms después de que aparezca el menú
              }

              // También mostrar el botón del menú completo
              const menuTrigger = document.querySelector(".menu-trigger");
              if (menuTrigger) {
                menuTrigger.style.transition = "all 0.3s ease-out";
                menuTrigger.style.opacity = "1";
                menuTrigger.style.transform = "translateY(0)";
              }

              // Mostrar el botón Únete
              const btnUnete = document.querySelector(".btn-unete");
              if (btnUnete) {
                btnUnete.style.transition = "all 0.3s ease-out";
                btnUnete.style.opacity = "1";
                btnUnete.style.transform = "translateY(0)";
                // Establecer clase inicial para hover normal
                btnUnete.classList.add("btn-unete-normal");
              }
            }, 200);

            // Permitir scroll y mostrar contenido
            document.body.classList.add("loaded");
          }, 500);
        }, 300);
      }, 600);
    }, 1200); // 1000 + 200ms del delay inicial
  }

  // Iniciar animaciones cuando GSAP esté listo o después de un timeout
  if (window.gsapReady) {
    initAnimations();
  } else {
    window.addEventListener("gsapReady", initAnimations);

    // Fallback: si después de 2 segundos GSAP no está disponible
    setTimeout(() => {
      if (!window.gsapReady) {
        initAnimations();
      }
    }, 2000);
  }
</script>

<style>
  .page-loader {
    backdrop-filter: blur(10px);
  }

  .loading-circle {
    pointer-events: none;
    /* Asegurar que esté oculto inicialmente pero permita animación */
    transform: scale(0);
    opacity: 0;
  }

  .menu-circle {
    transition:
      opacity 0.3s ease,
      transform 0.3s ease;
  }

  /* Ocultar contenido inicialmente */
  body {
    overflow: hidden;
  }

  body.loaded {
    overflow: auto;
  }

  /* Estilos para el header transparente */
  header {
    transition: all 0.3s ease;
  }

  /* Transición suave para el contenedor del header */
  .container {
    transition: padding 0.3s ease-in-out;
  }

  /* Estilos para los logos */
  .logo-container {
    position: relative;
  }

  .logo-hero,
  .logo-scroll {
    transition:
      opacity 0.5s ease-in-out,
      transform 0.3s ease-in-out;
  }

  .menu-circle,
  .menu-circle-mobile {
    transition: transform 0.3s ease-in-out;
  }

  /* Asegurar que el texto del menú sea visible sobre cualquier fondo */
  .menu-text,
  .menu-text-mobile {
    text-shadow: 0 1px 3px rgba(0, 0, 0, 0.3);
  }

  /* Estilos para el botón Únete */
  .btn-unete {
    transition: all 0.3s ease-in-out;
    border: none;
    cursor: pointer;
  }

  .btn-unete-normal {
    border: 2px solid white;
  }

  /* Hover cuando el botón es blanco (estado normal) */
  .btn-unete-normal:hover {
    background-color: #062bed !important;
    color: white !important;
    transform: scale(1.05);
    border: 2px solid #062bed;

  }

  .btn-unete-scrolled {
    background-color: transparent !important;
    color: #050835 !important;
    transform: scale(1.05);
    border: 2px solid #050835;
  }


  /* Hover cuando el botón es oscuro (estado scrolled) */
  .btn-unete-scrolled:hover {
    background-color: #ffffff !important;
    color: #062bed !important;
    transform: scale(1.05);
    border: 2px solid #062bed;

  }

  /* Mejorar la experiencia de scroll */
  html {
    scroll-behavior: smooth;
  }

  /* Asegurar que las secciones tengan un offset para el header fijo */
  section[id] {
    scroll-margin-top: 100px;
  }

  .menu-dropdown {
    top: 100%;
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
    border-top: 1px solid rgba(234, 234, 234, 0.3);
  }

  .menu-dropdown.menu-open {
    opacity: 1 !important;
    pointer-events: auto !important;
    transform: translateY(0) !important;
  }

  .menu-trigger {
    cursor: pointer;
  }

  .menu-trigger:hover .menu-circle {
    transform: scale(1.05);
    filter: brightness(1.1);
  }

  .menu-text {
    transition: all 0.3s ease;
  }

  /* Estilos del menú móvil y desktop */
  .mobile-menu-overlay,
  .desktop-menu-overlay {
    backdrop-filter: blur(10px);
  }

  /* Asegurar que los círculos decorativos no interfieran cuando el menú está cerrado */
  .mobile-menu-overlay,
  .desktop-menu-overlay {
    overflow: hidden;
  }

  .mobile-menu-overlay.translate-y-full,
  .desktop-menu-overlay.translate-y-full {
    pointer-events: none;
  }

  .mobile-menu-link,
  .desktop-menu-link {
    transition: all 0.3s ease;
  }

  .mobile-menu-link:hover .w-8,
  .desktop-menu-link:hover .w-9 {
    background-color: rgba(59, 130, 246, 0.2);
    border-color: #3b82f6;
  }

  .menu-circle-mobile {
    transition: all 0.3s ease;
  }

  .menu-trigger-mobile:hover .menu-circle-mobile {
    transform: scale(1.05);
    filter: brightness(1.1);
  }

  /* Asegurar que ambos menús estén por encima de todo */
  .mobile-menu-overlay,
  .desktop-menu-overlay {
    z-index: 9999;
  }

  @media (max-width: 768px) {
    .menu-dropdown {
      display: none;
    }

    /* Mostrar el círculo del menú móvil */
    .menu-circle-mobile {
      opacity: 1 !important;
    }
  }
</style>
